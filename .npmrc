# next-auth@5 (beta) declares a peerOptional on nodemailer ^7||^8 while the
# mail stack (mailparser, SMTP notifications) needs nodemailer 9. The peer is
# optional in practice — Servo never uses Auth.js's email provider — so relax
# strict peer resolution everywhere (local installs and CI npm ci alike).
legacy-peer-deps=true
